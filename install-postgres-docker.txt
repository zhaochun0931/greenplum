docker run --name mypg1 -e POSTGRES_PASSWORD=password -d postgres

docker exec -it mypg1 bash
su - postgres
psql postgres



the default postgres user and database are created in the entrypoint with initdb.
