# Use root/pivotal gpadmin/pivotal credentials

services:

  gp:
    image: datagrip/greenplum
    restart: always
    hostname: gp
    networks:
      - gp_network


networks:
  gp_network:
    driver: bridge
